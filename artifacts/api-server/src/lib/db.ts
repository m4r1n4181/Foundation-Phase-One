/**
 * Database connection singleton for the API server.
 * Imports from @workspace/db which owns the schema and connection pool.
 */
export { db } from "@workspace/db";
export * from "@workspace/db";
